# Backend base URL.
# Dev (Expo Go): leave as-is — the app auto-detects your computer's LAN IP from
#   the Metro bundler, so login works even when your WiFi IP changes.
# Production (EAS build): set this to your deployed server URL/domain.
EXPO_PUBLIC_API_URL=http://192.168.1.100:8080
